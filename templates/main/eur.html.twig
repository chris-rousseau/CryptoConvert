{% extends 'base.html.twig' %}

{% block title %}Convert'Crypto - €
{% endblock %}

{% block body %}
	<main>
		<header>
			<div class="img-logo">
				<img src="{{ asset('./assets/images/crypto.png') }}" alt="Name of the website" class="text-logo">
				<img src="{{ asset('./assets/images/logo.png') }}" alt="Logo of the website" class="logo">
			</div>
			<div class="devise-header">
				<a href="{{ path('form_usd') }}" class="devise--dollar">$</a>
				<a href="{{ path('form_eur') }}" class="devise--euro selected">€</a>
			</div>
		</header>
		<div class="convert">
			<form action="" method="post">
				<div class="form-convert">
					<label for="amount" class="visually-hidden">Amount:
					</label>
					<input type="text" name="devise" id="amount" placeholder="Amount in €..." required>
				</div>
				<div class="form-convert">
					<p>in
					</p>
				</div>
				<div class="form-convert">
					<label for="pet-select" class="visually-hidden">Crypto:
					</label>
					<select name="crypto" id="crypto-select">
						<option value="BTCEUR">Bitcoin</option>
						<option value="ETHEUR">Ethereum</option>
						<option value="XRPEUR">XRP</option>
						<option value="ADAEUR">Cardano</option>
						<option value="DOGEEUR">Dogecoin</option>
						<option value="BNBEUR">Binance Coin</option>
					</select>
				</div>
				<div class="form-convert">
					<input type="submit" value="Convert me please!">
				</div>
			</form>
		</div>
		{% if app.request.attributes.get('_route') == 'convert_eur' %}
			<div class="amount-convert">
				<p>
					<span class="result">{{ amountEUR }}€</span>
					=
					<span class="result">{{ amountCrypto }}
						{{ crypto }}</span>
				</p>
			</div>
		{% endif %}
	</main>

{% endblock %}
